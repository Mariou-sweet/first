# 输出前 100 个斐波那契数列

# 初始化前两个数
fibonacci = [0, 1]

# 生成斐波那契数列的前 100 个元素
for i in range(2, 100):
    next_num = fibonacci[-1] + fibonacci[-2]
    fibonacci.append(next_num)

# 打印结果
for index, num in enumerate(fibonacci, start=1):
    print(f"{index}: {num}")
